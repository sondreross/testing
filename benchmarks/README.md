## Benchmarks

Here are all the benchmarks taken from BEEBS benchmark suite, and used in the experiment.

Use the CMakeLists and pass either `-DSHORT_BENCH=OFF` or `-DSHORT_BENCH=ON` to build the long and short benchmarks, respectively. Building with cmake will result in a benchmarks-build directory with the ```.o``` files.